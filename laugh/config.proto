encoding "utf8"; // указываем кодировку, в которой написан конфигурационный файл

TTextMinerConfig {
  Dictionary = "params/mydic.gzt"; // путь к корневому словарю
  
  PrettyOutput = "PrettyOutputLaugh.html"; // путь к файлу с отладочным выводом в удобном для чтения виде

  Input = {
  	File = "master.txt";
  }

  Articles = [
    { Name = "смех" }
  ]
}
